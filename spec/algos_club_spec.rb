# frozen_string_literal: true

RSpec.describe AlgosClub do
  it "has a version number" do
    expect(AlgosClub::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
